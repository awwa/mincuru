{
  "name": "mincuru",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "generate-api-client": "rm -rf api-client;./node_modules/.bin/openapi-generator-cli generate -g typescript-axios -i openapi.yaml -o api-client",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.4.18"
  }
}
